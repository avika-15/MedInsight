# MedInsight
Med-INSIGHT is a deep learning–based medical image classification project using Python and TensorFlow. It applies transfer learning with a pre-trained VGG16 CNN to preprocess, augment, train, evaluate, and predict medical images, learning visual patterns from labeled datasets for analysis support.
